generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:senai@localhost:5432/projeto_industria")
}

model Aluno {
  id     Int    @id @default(autoincrement())
  nome   String
  turma  String
  idade  Int
}